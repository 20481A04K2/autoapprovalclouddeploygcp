apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: app-prod
spec:
  template:
    spec:
      containers:
      - image: app1
        resources:
          limits:
            cpu: 1000m
            memory: 128Mi
